#include <stdio.h>
#include <math.h> // math header file

int main ()
{
    double A = sqrt(9); // getting the square root of a number
    double B = pow(2, 4); // getting the power of a number.....similar to 2^4
    int C = round(3.14); // rounds off 3.14
    int D = ceil(3.14); // rounds a number up
    int E = floor(3.99); // rounds a number down
    double F = fabs(-100); // absolute value of a number...how far it is away from 0
    double G = log(3); // logarithms
    double H = sin(45); // sine
    double I = cos(45); // cosine
    double J = tan(45); // tangent

    printf ("%lf\n", J);

    return (0);
    
}