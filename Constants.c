#include <stdio.h>

int main()
{
    // Constant - A fixed value that cannot be altered by the program during its execution

    const float PI = 3.14159; // The keyword const provides constance and cannot change through the function

    printf("%f\n", PI);

    return (0);
    
}