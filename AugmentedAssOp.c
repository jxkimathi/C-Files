#include <stdio.h>

int main()
{

    // Augmented Assign Operators - Used to replace a statement where an operator takes a variable
    //                              as one of its arguments and then assigns the result back to same variable

    // these are just shortcuts that come in very handy when writing long code

    // x = x + 1
    // x += 1 (increments x by 1)

    int x = 10;

    // x = x + 2; 
    // x += 2;

    // x = x + 3;
    // x += 3;

    // x = x * 4;
    // x *= 4;

    // x = x / 5;
    // x /= 5;

    // x = x % 2;
    // x %= 2;

    printf("%d\n", x);

    return (0);
    
}